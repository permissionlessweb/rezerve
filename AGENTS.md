# AGENTS.md — re-zerve

Documentation is **`aep/`** only. Do not add `crate/product-docs`, `crate/book`, or a second docs tree.

Work on the lab compile host (`$REZERVE_LAB_HOST`), not a session laptop. After source edits: `sh scripts/sync-working-trees.sh` before cargo/go test.
